# Memory — 2026-03-28

## Session Activities
- **Cek saldo API**: DeepSeek $1.15, OpenRouter $3.05 used, semua API key aktif termasuk Sumopod (tapi tidak ada endpoint cek saldo otomatis)
- **Notifikasi GitHub ANTV SD**: Issue "Find: ANTV SD" dari repo iptv-org/iptv, sudah di-mark as read dan unsubscribe agar tidak muncul lagi di laporan
- **Masalah model Sumopod**: Di Telegram command `/models` hanya menampilkan 1 model Sumopod padahal config sudah ada 41 model. Model sudah sync ke semua agent (41 model di main agent dan agent1), tapi mungkin ada bug/cache di command `/models`

## Issues & Solutions
1. **Sumopod balance check**: Tidak ada API endpoint otomatis, perlu cek manual di dashboard https://ai.sumopod.com
2. **GitHub notifications cleanup**: Notifikasi ANTV SD sudah di-handle, tidak akan muncul lagi
3. **Model display bug**: Model Sumopod 41 sudah ada di config tapi tidak ditampilkan dengan benar di Telegram `/models` command

## Technical Notes
- Gateway restart dilakukan untuk refresh config model Sumopod
- Script `add-provider-models.sh` berhasil sync 41 model Sumopod ke semua 9 agent
- Masih perlu investigasi mengapa Telegram `/models` hanya menampilkan 1 model Sumopod

## WhatsApp Monitoring System
- Mas Aris menunjukkan screenshot WhatsApp dengan sistem monitoring otomatis yang komprehensif
- Sistem melaporkan status: Claude 3.7 Opus "Terhubung normal", Perplexity Realtime "Online", Monitor Radar ADS‑B, Market and Selat Hormuz monitors "Aktif", automatic backups setiap 15 menit
- Semua sistem berjalan normal dengan checklist hijau
- Ini menunjukkan adanya sistem monitoring enterprise/pribadi yang sophisticated yang dibuat dengan automation (mungkin OpenClaw atau tools lain)

## Perplexity AI Knowledge
- Perplexity adalah AI search engine dengan real-time search dan citations
- Beda dengan ChatGPT: Perplexity selalu up-to-date dengan web, kasih sumber
- Setup Perplexity API: butuh API key berbayar ($20/month), tambah ke auth-profiles.json
- Model tersedia: sonar, sonar-pro, sonar-reasoning
- Berguna untuk monitoring real-time data, research, fact-checking## 2026-03-28 15:57 UTC - Gateway Troubleshooting & Media Download

### Gateway Status Recovery
- Gateway sebelumnya unreachable dengan error "missing scope: operator.read"
- Setelah restart gateway (`openclaw gateway restart`), status kembali normal: RPC probe ok, state active
- Error "missing scope: operator.read" muncul karena gateway bind ke loopback (127.0.0.1) dan tidak ada operator config
- Ini NORMAL untuk gateway loopback-only (bukan error permission)

### Media Download Issue
- User melaporkan "⚠️ Failed to download media. Please try again."
- Gateway berjalan normal setelah restart
- Folder `/root/.openclaw/media/inbound/` berisi banyak file media (38+ file)
- Media baru berhasil diterima (screenshot chat Santa LDP)
- Permintaan user: "Ubah gambar JD text" (butuh klarifikasi lebih detail)

### Pelajaran
- `openclaw gateway status` adalah cara benar cek gateway (bukan `openclaw status` yang sering tampilkan "missing scope: operator.read")
- Error "missing scope: operator.read" bukan masalah permission, tapi normal untuk loopback bind
- Media download mungkin gagal karena network timeout atau file size, bukan gateway issue
